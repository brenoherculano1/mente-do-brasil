{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "fastapi",
  "regions": ["gru1"],
  "functions": {
    "app.py": {
      "excludeFiles": "{tests/**,docs/**,audit_packages/**,audit_results/**,artifacts/**,data/raw/**,data/staging/**,data/releases/**,data/web/**,web/.next/**,web/node_modules/**,**/*.test.py,**/test_*.py,backups/**}"
    }
  }
}
