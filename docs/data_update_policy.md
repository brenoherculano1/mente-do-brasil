# Data Update Policy

Mente do Brasil does not mutate public data from live sources. Planned periodic or annual releases depend on authoritative source availability, acquisition validation, method lock, scientific regression, deterministic artifact build, privacy/legal review, and QA. No frequency is guaranteed. Each update receives a new immutable release ID and retains prior releases unless an exceptional takedown is required.
