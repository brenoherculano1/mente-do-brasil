# Metrics Pipeline

Reserved for indicator rates, percentile ranks, need/capacity composites, and mismatch score generation.

Do not create new indicators or change locked weights without a new `method_version`.
